HTML:
块级元素和行内元素都有哪些？他们之间的区别是什么。

HTML5中有哪些新标签，属性。


css:
介绍一下css盒模型，介绍box-sizing

列举一下你所知道的css hack ，比如那些是ie6专用的，那些是ie6-8用的。

css3里边有哪些新属性，解释下@media screen;

js:

写一个简单的ajax工具函数，例如：request(url,option)

跨域请求有哪些方案，说下jsonp的实现原理

js有哪些实现继承的方式，说下闭包是什么

写一个函数实现把手机号码格式化 18888888888 => 188 8888 8888

写出以下代码的输出结果

var length = 10;

function fn(){
    console.log(this.length);
}

var obj = {
  length:5,
  method:function(fn){
    fn();//?
    arguments[0]();


  }
};

obj.method(fn);

写出以下代码的输出结果

var reg = /abc/g;
var str = 'abcd';

reg.test(str)//？
reg.test(str)//?